function isArmstrongNumber(num : number) :any {
    var str = num.toString, output = [];
    num.valueOf
    for(var i = 0; i < str.length; i++ ){
        output.push(+str.cha);
    }
    if (num <10 && )
}